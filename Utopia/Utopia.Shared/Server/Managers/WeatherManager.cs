namespace Utopia.Shared.Server.Managers
{
    public class WeatherManager
    {
        private readonly ServerCore _server;

        public WeatherManager(ServerCore server)
        {
            _server = server;
        }
    }
}
