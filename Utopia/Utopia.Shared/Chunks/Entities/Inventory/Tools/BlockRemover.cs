
#region Using Statements
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

#endregion

namespace Utopia.Shared.Chunks.Entities.Inventory.Tools
{
    public class BlockRemover : Tool
    {

       /* public BlockRemover(int id, String name, Player player, VoxelModel model)
            : base(id, name, player, model) { }
       
        public override BlockType getToolType() { return BlockType.None; }

        public override bool Use()
        {

            if (currentSelection.HasValue)
            {
                player.world.SetBlockAndReBuild(currentSelection.Value.Position, BlockType.None);
                return true;
            }
            return false;
        
        }

        */
    }
}
